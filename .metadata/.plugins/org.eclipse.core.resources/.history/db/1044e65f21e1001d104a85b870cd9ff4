package in.yh.controllers;

import java.io.IOException;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import in.yh.dao.LoginDao;
import in.yh.dao.UserDao;
import in.yh.pojo.User;

/**
 * Servlet implementation class AdminLogin
 */
@WebServlet("/AdminLogin")
public class AdminLogin extends HttpServlet {
	boolean result;
	LoginDao ldao=new LoginDao();
	HttpSession session=null;
	String action=null;
	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		action=request.getParameter("action");
		session=request.getSession();
		System.out.println(action+ " " +session);
		if(session!=null) {
			session.invalidate();
			response.sendRedirect("/YouHeal/jsp/index.jsp");
		}
	}

	/**
	 * @see HttpServlet#doPost(HttpServletRequest request, HttpServletResponse response)
	 */
	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		
		   action=request.getParameter("action");
		   System.out.println(action);
			if(action!=null && action.equals("adminlogin")) {
			String email=request.getParameter("email");
			String password=request.getParameter("password");
			result=ldao.isAdmin(email, password);
			session=request.getSession();
			if(result) {
				session.setAttribute("user",email );
				response.sendRedirect("/YouHeal/admin/index.jsp");
			}
			else {
				session.setAttribute("errmsg","Invalid Email and Password" );
				response.sendRedirect("/YouHeal/jsp/adminlogin.jsp");
			}
		
		}// end of admin login
			
			
			
			if(action!=null && action.equals("userlogin")) {
				String name=request.getParameter("name");
				String email=request.getParameter("email");
				String password=request.getParameter("password");
				result=ldao.isUser(email, password);
				User user=new User(name,email,password);
				session=request.getSession();
				if(result) {
					session.setAttribute("user",user );
					response.sendRedirect("/YouHeal/jsp/index.jsp");
				}
				else {
					session.setAttribute("errmsg","Invalid Email and Password" );
					response.sendRedirect("/YouHeal/jsp/userlogin.jsp");
				}
			
			}
	}

}
